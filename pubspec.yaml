name: general_flutter_widgets
description: A collection of reusable Flutter widgets including JuicyButton, GlassContainer, AdaptiveLayout, SpacedFlex, and custom cursor thermal effects.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.65.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
